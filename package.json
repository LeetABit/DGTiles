{
  "name": "dgtiles",
  "private": true,
  "dependencies": {
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "yarn-or-npm": "3.0.1",
    "react-scripts": "4.0.3"
  },
  "scripts": {
    "generate": "node update-version.mjs",
    "start": "yon run generate && react-scripts start",
    "build": "yon run generate && react-scripts build",
    "test": "yon run generate && react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
