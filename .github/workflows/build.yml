name: Build

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  build_and_deploy_job:
    name: Build and Deploy Job
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0

      - name: Get version
        id: gitVersion
        if: github.event_name == 'push'
        uses: actions/github-script@v5
        with:
          github-token: ${{ github.token }}
          script: |
            const calculateGitVersion = require('./calculate-version.js');
            const gitVersion = await calculateGitVersion();
            return gitVersion.version;

      - name: Create tag
        if: github.event_name == 'push'
        uses: actions/github-script@v5
        with:
          github-token: ${{ github.token }}
          script: |
            github.rest.git.createRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: "refs/tags/v" + ${{steps.gitVersion.outputs.result}},
              sha: context.sha
            })

      - name: Fetch tag
        if: github.event_name == 'push'
        run: git fetch --tags

      - name: Cache node modules
        uses: actions/cache@v2
        with:
          path: ./node_modules
          key: app-node-modules-${{ hashFiles('./yarn.json') }}
          restore-keys: |
            app-node-modules-

      - name: Build And Deploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "/"
          api_location: ""
          output_location: "build"

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          action: "close"
